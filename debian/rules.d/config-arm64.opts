# arm64-specific kernel config overrides for Proxmox VE
#
# The base config comes from Ubuntu's arm64 annotations which already
# include good server defaults (NUMA, ACPI, EFI, KVM, NR_CPUS=512).
# Common PVE options are in config-common.opts.
